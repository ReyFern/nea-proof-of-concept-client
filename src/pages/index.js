import React from "react";
import Menu from "../components/Menu.js";

const Home = () => {
    return (
        <div className="Home">
            <Menu/>
        </div>
    );
};

export default Home;