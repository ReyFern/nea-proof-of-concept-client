import React from "react";

const Dictionary = () => {
    return (
        <div className="Dictionary">
            <h1>This is a library of DND species, classes, backgrounds, items, and spells.</h1>
        </div>
    );
};

export default Dictionary;